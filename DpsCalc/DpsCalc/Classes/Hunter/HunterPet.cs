using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace demo
{
    class HunterPet
    {
        int strength;
        int agility;
        int spirit;
        int damageMin;
        int damageMax;
        double attackSpeed;
        double dmgpersec;
        
        public HunterPet(Pet k, int str, int agi, int sta, int intel, int spi, int ap, int dmgMin, int dmgMax, double atkspeed, double dps, int sp, int ar)
        {
            //kind = k;
            strength = str;
            agility = agi;
            //baseStamina = sta;
            //baseIntelect = intel;
            spirit = spi;
            //baseAttackPower = ap;
            damageMin = dmgMin;
            damageMax = dmgMax;
            attackSpeed = atkspeed;
            dmgpersec = dps;
            //baseSpellPower = sp;
            //baseArmor = ar;
        }

        public HunterPet(CunPet k, int str, int agi, int sta, int intel, int spi, int ap, int dmgMin, int dmgMax, double atkspeed, double dps, int sp, int ar)
        {
            //kind = k;
            strength = str;
            agility = agi;
            //baseStamina = sta;
            //baseIntelect = intel;
            spirit = spi;
            //baseAttackPower = ap;
            damageMin = dmgMin;
            damageMax = dmgMax;
            attackSpeed = atkspeed;
            dmgpersec = dps;
            //baseSpellPower = sp;
            //baseArmor = ar;
        }

        public HunterPet(FeroPet k, int str, int agi, int sta, int intel, int spi, int ap, int dmgMin, int dmgMax, double atkspeed, double dps, int sp, int ar)
        {
            //kind = k;
            strength = str;
            agility = agi;
            //baseStamina = sta;
            //baseIntelect = intel;
            spirit = spi;
            //baseAttackPower = ap;
            damageMin = dmgMin;
            damageMax = dmgMax;
            attackSpeed = atkspeed;
            dmgpersec = dps;
            //baseSpellPower = sp;
            //baseArmor = ar;
        }

        public HunterPet(TenaPet k, int str, int agi, int sta, int intel, int spi, int ap, int dmgMin, int dmgMax, double atkspeed, double dps, int sp, int ar)
        {
            //kind = k;
            strength = str;
            agility = agi;
            //baseStamina = sta;
            //baseIntelect = intel;
            spirit = spi;
            //baseAttackPower = ap;
            damageMin = dmgMin;
            damageMax = dmgMax;
            attackSpeed = atkspeed;
            dmgpersec = dps;
            //baseSpellPower = sp;
            //baseArmor = ar;
        }
    }
}